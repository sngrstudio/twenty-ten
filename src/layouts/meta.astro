---
import type { CollectionEntry } from 'astro:content'

export interface Props {
  title?: string | undefined
  description?: string | undefined
  canonical?: string | undefined
  site: CollectionEntry<'site'>
}

const { title, description, canonical, site } = Astro.props
---

{/* Basic metadata */}
<title>{title ? `${title} \u2014 ${site.data.title}` : site.data.title}</title>
<meta
  name='description'
  content={description ? description : site.data.description}
/>
<link rel='canonical' href={canonical ? canonical : Astro.url} />

{/* Favicon */}
<link rel='icon' type='image/svg+xml' href='/favicon.svg' />
